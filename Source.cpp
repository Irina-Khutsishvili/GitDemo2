#include<iostream>
#include<vector>
#include<string>
using namespace std;
/*
template<typename A>			// saTauri: 1.5 => const  + 0.5;  & + 0.5; tipi A + 0.5
void printVec(constvector<A>& x){
	for (auto i = 0; i < x.size(); ++i){
		if (i % 3 == 0)cout << endl;	// tani: 2 =>piroba + 1; danarCeni + 1
		cout << x[i] << '\t';
	}
	cout << endl;
}
template<typename A>
void inputVec(vector<A>& x, int s){
	A obj;
	for (int i = 1; i <= s; ++i){
		cin >> obj;
		x.push_back(obj);
	}
}
*/
int main()
{
	vector<int> I { 1, 2, 3, 4, 5, 6, 7, 8 };  // 0.5
	//intVec(I);							// 0.5

	
}
